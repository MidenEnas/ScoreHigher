<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Score Higher</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Admin Dashboard</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/admin/users">Users</a>
            <form action="/logout" method="POST" style="display: inline; margin: 0;">
                <button type="submit" class="logout-btn">Logout</button>
            </form>
        </nav>
    </header>
    <main>
        <h2>Manage Competitions</h2>
        
        <div class="admin-actions" style="margin: 2rem 0;">
            <form action="/admin/create-competition" method="GET" style="display: inline-block;">
                <button type="submit" class="admin-button">Create New Competition</button>
            </form>
        </div>
        
        <h3>Existing Competitions</h3>
        <% if (competitions && competitions.length > 0) { %>
            <div class="competitions-list">
                <% competitions.forEach(comp => { %>
                    <div class="admin-competition-card">
                        <div>
                            <strong><%= comp.name %></strong>
                            <span style="color: #6a5a4a; margin-left: 1rem;"><%= new Date(comp.date).toLocaleDateString() %></span>
                        </div>
                        <form action="/admin/competition/<%= comp.id %>" method="GET" style="margin: 0;">
                            <button type="submit" class="admin-button-small">Manage</button>
                        </form>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No competitions yet.</p>
        <% } %>
    </main>
</body>
</html>